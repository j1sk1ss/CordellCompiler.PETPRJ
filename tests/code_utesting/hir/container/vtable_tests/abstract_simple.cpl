container interface {
    @[abstract] @[self]
    function init(ptr interface self) -> i0;
}

start() {
    interface i;
}

:/ OUTPUT
{
    fn _main()
    {
        {
            u8s %1 = arr_alloc(csu8 8);
        }
    }
}
/:
