interface int {
    @[abstract] @[self]
    function init(ptr int self) -> i0;
}

start() {
    int i;
}

:/ OUTPUT
{
    fn _main()
    {
        {
            u8s %1 = arr_alloc(csu8 8);
        }
    }
}
/:
